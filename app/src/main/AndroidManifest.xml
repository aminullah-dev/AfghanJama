<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- فقط برای اشتراکِ کارگاه روی وای‌فایِ محلی؛ اپ به اینترنت کاری ندارد -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        allowBackup پیش‌فرضِ اندروید true است؛ یعنی با `adb backup` روی یک
        گوشیِ بازشده می‌شود کلِ دیتابیس — دفترِ مالی، بدهیِ مشتری‌ها،
        حقوقِ کارکنان — را بیرون کشید. برای این اپ بسته شد.

        قابلیتی از دست نمی‌رود: اپ پشتیبان‌گیریِ خودش را دارد (زیپ با
        عکس‌ها، دستی و خودکار) که کاربر آگاهانه می‌گیرد و جایش را
        خودش انتخاب می‌کند.
    -->
    <application
        android:name=".App"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="AfghanJama"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AfghanJama">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            windowSoftInputMode: بی این، کیبورد روی کادرهای پایینِ صفحه
            می‌افتد و کاربر جایی را که تایپ می‌کند نمی‌بیند — در اپی که
            کارش پر کردنِ فرم است، آزاردهنده. adjustResize صفحه را جمع
            می‌کند تا کادرِ فعال بالای کیبورد بمانَد.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
