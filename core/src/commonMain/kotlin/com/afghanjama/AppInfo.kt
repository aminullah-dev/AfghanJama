package com.afghanjama

/**
 * هویتِ خودِ برنامه — جدا از هویتِ کارگاهی که از آن استفاده می‌کند.
 *
 * تا امروز این دو به هم چسبیده بودند. نامِ «افغان‌جامه» هم نامِ اپ بود و
 * هم روی فاکتور به‌جای نامِ فروشنده می‌نشست؛ یعنی هر کارگاهی که این اپ
 * را می‌گرفت، فاکتورش را به نامِ کارگاهِ دیگری صادر می‌کرد.
 *
 * قاعده از این به بعد:
 *
 * - **نامِ برنامه** ([NAME]) جایی می‌آید که حرف از خودِ نرم‌افزار است:
 *   صفحهٔ ورود، عنوانِ گزارشِ خودآزمایی، سطرِ «صادرشده با…» ته اسناد.
 * - **نامِ کارگاه** (`CompanyPrefs.name`) هر جایی که حرف از کسب‌وکار
 *   است: سرصفحهٔ فاکتور و رسید، فروشندهٔ فاکتور، تابلوی کارگاه.
 *
 * جای این دو را با هم عوض نکنید؛ همان اشتباهی بود که اصلاح شد.
 *
 * نامِ بسته (`com.afghanjama`) عمداً دست‌نخورده مانده. کاربر هیچ‌وقت
 * آن را نمی‌بیند، و عوض کردنش اپ را از نظرِ اندروید یک برنامهٔ تازه
 * می‌کند: نسخهٔ نصب‌شده روی گوشیِ کارگاه دیگر آپدیت نمی‌گیرد و داده‌اش
 * فقط از راهِ پشتیبان برمی‌گردد.
 */
object AppInfo {

    /** نامِ فارسیِ برنامه — هرجا کاربر می‌بیند. */
    const val NAME = "خیاط‌یار"

    /**
     * همان نام با حروفِ لاتین، برای جایی که نامِ فارسی دردسر می‌سازد:
     * نامِ پوشه در حافظه و نامِ فایلِ خروجی. بعضی مدیرهای فایل و
     * سرویس‌های اشتراک با نامِ راست‌به‌چپ بد رفتار می‌کنند.
     */
    const val NAME_LATIN = "KhayatYar"
}
