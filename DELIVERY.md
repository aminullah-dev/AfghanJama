# آماده‌سازی تحویل

## نسخهٔ درست را تحویل بدهید

هر ساخت دو فایل می‌سازد:

| فایل | برای چه |
|---|---|
| `app-debug.apk` | فقط برای آزمایشِ خودتان |
| `app-release.apk` | **این را تحویل بدهید** |

نسخهٔ دیباگ `debuggable` است: هرکس با یک کابل USB می‌تواند دیباگر وصل
کند و دفترِ مالی، بدهیِ مشتری‌ها و حقوقِ کارکنان را بخواند یا عوض کند.
با کلیدِ دیباگ هم امضا می‌شود که کلیدی همگانی است — یعنی هر کسی
می‌تواند «آپدیت» بسازد و روی اپِ کارگاه بنشاند. برای تحویل مناسب نیست.

## کلیدِ امضا

نسخهٔ release بدونِ کلید ساخته می‌شود ولی **امضا نمی‌شود** و روی گوشی
نصب نمی‌شود. کلید مالِ شماست و هرگز نباید در مخزن بنشیند.

یک بار بسازید و **جای امنی نگه دارید**:

    keytool -genkey -v -keystore afghanjama.jks \
      -keyalg RSA -keysize 2048 -validity 10000 -alias afghanjama

> اگر این فایل یا رمزش گم شود، دیگر هرگز نمی‌توانید برای همین اپ آپدیت
> بدهید. کاربر باید اپ را پاک کند و از نو نصب — یعنی دادهٔ کارگاه از
> بین می‌رود مگر از پشتیبان برگردانده شود. یک نسخهٔ پشتیبان از خودِ
> کلید هم جای دیگری نگه دارید.

بعد در GitHub → Settings → Secrets and variables → Actions چهار تا
اضافه کنید:

| نام | مقدار |
|---|---|
| `KEYSTORE_BASE64` | خروجیِ `base64 -w0 afghanjama.jks` |
| `KEYSTORE_PASSWORD` | رمزِ فایلِ کلید |
| `KEY_ALIAS` | `afghanjama` |
| `KEY_PASSWORD` | رمزِ خودِ کلید |

از آن به بعد هر ساخت، APK امضاشده و آمادهٔ نصب می‌دهد.

## پیش از تحویل

- [ ] روی یک **گوشیِ واقعی** نصب و آزمایش شود، نه فقط شبیه‌ساز
- [ ] کادرهای ورودی کار کنند (تایپ و کیبورد)
- [ ] یک فاکتور کامل ثبت شود و در گزارش‌ها دیده شود
- [ ] پشتیبان گرفته و همان‌جا برگردانده شود
- [ ] `versionCode` و `versionName` در `app/build.gradle.kts` برای این
      تحویل به‌روز باشد (هر تحویلِ تازه باید `versionCode` بزرگ‌تر
      داشته باشد وگرنه روی نسخهٔ قبلی نصب نمی‌شود)

## کوچک‌سازی (R8)

عمداً خاموش است. Room، Compose، zxing و biometric با بازتاب کار
می‌کنند و R8 می‌تواند چیزی را در **زمانِ اجرا** بشکند، نه هنگامِ ساخت.
روشن کردنش بدونِ آزمونِ کاملِ دستی روی گوشی یعنی خطرِ کرش سرِ مشتری.
`app/proguard-rules.pro` پایه‌اش را دارد؛ اگر روزی روشن شد، همهٔ
مسیرها باید دوباره دستی آزموده شوند.
