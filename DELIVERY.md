# ساختن نسخهٔ نصبی برای کارفرما

نسخهٔ نصبی روی **کامپیوتر خودتان** در Android Studio ساخته می‌شود.
کلید امضا هرگز به GitHub نمی‌رود — نه در مخزن، نه در Secrets.

**گیت‌هاب APK نمی‌سازد.** با هر push فقط بررسی‌ها و آزمون‌های پول
می‌دوند (`checks.yml`، حدود شش دقیقه). ساختِ APK و آزمونِ شبیه‌ساز در
`apk.yml` است و **فقط دستی** اجرا می‌شود — Actions ← «ساخت APK» ← Run
workflow. اگر همیشه خودتان در Android Studio خروجی می‌گیرید، لازم نیست
هرگز به آن دست بزنید.

---

## طرحِ دیتابیس — انجام شده، ولی بدانید چرا هست

`DB_VERSION` الان **۶۳** است و طرحِ هر سه نسخه در گیت کامیت شده:

    app/schemas/com.afghanjama.data.AppDatabase/{61,62,63}.json
    desktop/schemas/com.afghanjama.desktop.data.DesktopDatabase/{61,62,63}.json

**کارِ شما نیست مگر `DB_VERSION` را بالا ببرید.** ولی اگر روزی بردید،
Room طرحِ نسخهٔ تازه را فقط **هنگام ساخت** می‌نویسد و آن فایل باید
کامیت شود، وگرنه `schemacheck` قرمز می‌ماند و مهاجرت آزمودنی نیست.

دو راه دارید:

1. **خودتان:** `Build → Rebuild Project`، بعد فایلِ تازه را کامیت کنید.
   اگر ساخته نشد، `build/` را پاک کنید و دوباره بسازید — `DB_VERSION`
   یک `const val` است و ساختِ افزایشی گاهی KSP را دوباره اجرا نمی‌کند.

2. **از راهِ CI:** پله‌ای در `checks.yml` هست که اگر طرحِ نسخهٔ جاری
   کامیت نشده باشد، بستهٔ فشرده‌اش را در لاگ می‌گذارد تا برداشته شود.
   خودش را خاموش می‌کند وقتی کامیت شد، و اگر نسخه بالا رفت دوباره
   روشن می‌شود.

---

## گام ۱ — کلید بسازید (فقط یک بار، برای همیشه)

در Android Studio:

**Build → Generate Signed App Bundle / APK…**

1. **APK** را انتخاب کنید (نه App Bundle — آن برای Google Play است؛
   شما فایل را مستقیم به کارفرما می‌دهید) → Next
2. زیر Key store path بزنید **Create new…**
3. پر کنید:

   | خانه | چه بنویسید |
   |---|---|
   | Key store path | جایی **بیرونِ پوشهٔ پروژه** (مثلاً `~/keys/afghanjama.jks`) |
   | Password | یک رمز قوی |
   | Alias | `afghanjama` |
   | Key password | یک رمز قوی |
   | Validity | ۲۵ سال یا بیشتر |
   | First and Last Name / Organization | نام خودتان یا کارگاه |

4. OK

> **مهم:** کلید را بیرونِ پوشهٔ پروژه بسازید. اگر داخلش باشد ممکن است
> اشتباهی به گیت برود. (`.gitignore` حالا جلویش را می‌گیرد، ولی
> بیرون گذاشتنش مطمئن‌تر است.)

### این فایل را گم نکنید

از `afghanjama.jks` و رمزهایش **یک پشتیبان جای دیگری** بگذارید — فلش،
هارد دیگر، جای امن.

اگر گم شود، دیگر **هرگز** نمی‌توانید به این اپ آپدیت بدهید. اندروید
آپدیتی را که با کلید دیگری امضا شده قبول نمی‌کند. کارفرما باید اپ را
پاک کند و از نو نصب — یعنی همهٔ دادهٔ کارگاه می‌رود، مگر از پشتیبانِ
خودِ اپ برگردانده شود.

---

## گام ۲ — APK را بسازید

باز هم **Build → Generate Signed App Bundle / APK… → APK → Next**

1. کلیدی که ساختید را انتخاب کنید و رمزها را بزنید
   (تیک Remember passwords را بزنید تا دفعهٔ بعد نپرسد)
2. Build Variant: **release** ← این مهم‌ترین قسمت است
3. Create

فایل اینجا ساخته می‌شود:

    app/release/app-release.apk

**همین فایل را به کارفرما بدهید.**

---

## debug و release چه فرقی دارند

| | debug | release |
|---|---|---|
| برای | آزمایش خودتان | **تحویل کارفرما** |
| debuggable | بله — با کابل USB می‌شود دفتر مالی را خواند و عوض کرد | نه |
| امضا | کلید دیباگ، در تمام دنیا یکی | کلید خودتان |

نسخهٔ دیباگ را به کارفرما ندهید. با کلید دیباگ هر کسی می‌تواند یک
«آپدیت» بسازد و روی اپ کارگاه بنشاند، چون گوشی آن امضا را می‌شناسد.

---

## نشانِ اپ — یک کارِ نیمه‌تمام که خودتان تمامش کنید

نشانِ اپ عوض شد: پیراهنِ سفید روی سبزِ خودِ برنامه، به‌جای رباتِ سبزِ
پیش‌فرضِ Android Studio. این نشان **برداری** است و روی اندروید ۸ به
بعد (یعنی عملاً همهٔ گوشی‌های امروز) درست دیده می‌شود.

ولی اندروید ۷.۰ و ۷.۱ نشانِ برداری نمی‌گیرند و از فایل‌های تصویریِ
`mipmap-*/ic_launcher.webp` می‌خوانند — و آن‌ها **هنوز رباتِ پیش‌فرض
هستند**، چون ساختنشان به ابزارِ تصویری نیاز دارد که در محیطِ ساخت
نبود.

اگر می‌خواهید روی گوشی‌های خیلی قدیمی هم نشانِ درست دیده شود، در
Android Studio سی ثانیه کار است:

**راست‌کلیک روی `app/src/main/res` → New → Image Asset**

- Icon Type: `Launcher Icons (Adaptive and Legacy)`
- تبِ Background: رنگِ ثابت `#1F6E5C`
- تبِ Foreground: `Asset Type: Image` و فایلِ
  `app/src/main/res/drawable/ic_launcher_foreground.xml` را بدهید
- Next → Finish

این کار فقط فایل‌های `.webp` را از نو می‌سازد؛ نشانِ گوشی‌های جدید
عوض نمی‌شود چون از همان بردار می‌آید.

---

## گام ۳ — پیش از تحویل امتحان کنید

روی یک **گوشی واقعی**، نه شبیه‌ساز:

- [ ] نصب می‌شود و باز می‌شود، و نشانِ اپ روی صفحهٔ گوشی سبزِ خودمان است
- [ ] در کادرها می‌شود تایپ کرد و کیبورد بالا می‌آید
- [ ] در کادرهای مقدار (مصرفِ پارچه، موجودی، ضایعات) کیبورد **نقطه**
      دارد و `2.5` قبول می‌شود
- [ ] یک سفارش کامل: ثبت → برش → دوخت → نظارت → انبار
- [ ] تحویلِ جزئی: از ۱۰ عدد، ۵ عدد جلو برود و ۵ عدد نزد خیاط بماند
- [ ] یک فاکتور فروش ثبت شود و در گزارش‌ها و دفتر مشتری دیده شود
- [ ] **برگشت از فروش:** از ۱۰ عدد، ۲ عدد برگشت داده شود؛ در گزارش‌ها
      باید «فروش ناخالص − برگشت = خالص» دیده شود، نه همان ۱۰
- [ ] **لوگوی کارگاه:** در تنظیمات انتخاب شود، و بعد از **بستن و دوباره
      باز کردنِ اپ** هنوز سرِ جایش باشد
- [ ] پشتیبان گرفته شود و همان‌جا برگردانده شود
- [ ] اسکنر QR و چاپ فاکتور کار کنند
- [ ] تنظیمات → «خودآزمایی و سلامتِ داده» اجرا شود و همه سبز باشد

---

## گام ۴ — ریلیز، تا دکمه‌های سایت کار کنند

صفحهٔ محصول در `docs/site/` سه دکمهٔ دانلود دارد که **از قبل** به این
نشانی‌ها اشاره می‌کنند:

    .../releases/download/v1.7.0/KhayatYar-1.7.0.apk
    .../releases/download/v1.7.0/KhayatYar-1.7.0.msi

تا وقتی ریلیز منتشر نشده، هر سه به ۴۰۴ می‌روند. برای زنده کردنشان:

**۱. دو فایل را آماده کنید**

| فایل | از کجا | نامش باید باشد |
|---|---|---|
| APK | گامِ ۲ بالا (`app/release/app-release.apk`) | `KhayatYar-1.7.0.apk` |
| MSI | Actions ← آخرین اجرای سبزِ «بستهٔ ویندوز» ← Artifacts | `KhayatYar-1.7.0.msi` |

**۲. ریلیز بسازید**

`github.com/aminullah-dev/AfghanJama/releases` ← **Draft a new release**
← تگ `v1.7.0` ← هر دو فایل را بکشید و رها کنید ← **Publish release**.

> نام‌ها باید **دقیقاً** همان بالا باشند. یک حرفِ اضافه یعنی دکمه به
> ۴۰۴ می‌رود. اگر نامِ دیگری خواستید، لینک‌های داخلِ دو فایلِ HTML هم
> باید عوض شوند — شش جا.

**چرا ریلیزِ گیت‌هاب و نه کتابخانهٔ رسانهٔ وردپرس:** نصب‌کنندهٔ ویندوز
~۱۲۸ مگابایت است و وردپرس روی بیشترِ هاست‌ها ۶۴ مگابایت یا کمتر
می‌پذیرد — ضمناً پسوندِ `.apk` و `.msi` را اصلاً در فهرستِ مجاز ندارد.
و نمی‌شود مستقیم به آرتیفکتِ CI لینک داد: آرتیفکت برای عموم قابلِ
دانلود نیست (ورود لازم دارد) و بعد از ۳۰ روز پاک می‌شود.

**نسخهٔ بعدی:** تگِ تازه (`v<نسخه>`)، نام‌های تازه، و در دو فایلِ HTML
`1.7.0` را با نسخهٔ تازه عوض کنید. جزئیاتِ بیشتر در
`docs/site/گذاشتن-روی-سایت.md`.

---

## آپدیت‌های بعدی

نسخهٔ فعلی **۱.۷.۰** است (`appVersionCode = 12`).

هر بار که نسخهٔ تازه‌ای می‌دهید، **فقط یک فایل** عوض می‌شود:
`gradle.properties`.

```properties
appVersion=1.7.0        # چیزی که کاربر می‌بیند — سه بخشِ عددی
appVersionCode=12       # فقط اندروید؛ هر بار یکی بیشتر
```

اپِ اندروید و نسخهٔ ویندوز هر دو از همین می‌خوانند. پیش‌تر نسخه دو جا
نوشته می‌شد (`app/build.gradle.kts` و `desktop/build.gradle.kts`) و از
هم افتاده بود: اندروید `1.0` بود و ویندوز `1.0.0`.

**چرا هر دو عدد باید بالا بروند:**

- `appVersionCode` بالا نرود → گوشی آپدیت را **نصب نمی‌کند**.
- `appVersion` بالا نرود → MSIِ تازه روی نصب‌شده **نمی‌نشیند**. ویندوز
  فقط وقتی جایگزین می‌کند که عدد بیشتر باشد.

قالبِ `appVersion` باید سه بخشِ عددی باشد (دو بخشِ اول تا ۲۵۵، سومی تا
۶۵۵۳۵) — این محدودیتِ خودِ نصب‌کنندهٔ ویندوز است. بررسیِ `appversion`
همه‌اش را وارسی می‌کند؛ **جز یک چیز:** نمی‌تواند بگوید نسبت به بستهٔ
قبلی بالا رفته یا نه. آن به تاریخچه نیاز دارد و مسئولیتش با شماست.

و APK را **با همان کلید قبلی** امضا کنید.

---

## نکته‌های فنی

**کوچک‌سازی (R8) عمداً خاموش است.** Room، Compose، zxing و biometric با
بازتاب کار می‌کنند؛ R8 می‌تواند چیزی را در *زمان اجرا* بشکند، نه هنگام
ساخت. یعنی اپ ساخته می‌شود ولی سرِ کارفرما کرش می‌کند. اگر روزی روشنش
کردید، همهٔ مسیرهای بالا را دوباره دستی امتحان کنید.
`app/proguard-rules.pro` پایه‌اش را دارد.

**پشتیبان‌گیریِ سیستم بسته است** (`allowBackup="false"`). یعنی با
`adb backup` نمی‌شود دیتابیس را از گوشی بیرون کشید. انتقال داده باید با
پشتیبانِ خودِ اپ انجام شود — که کاربر آگاهانه می‌گیرد.

**GitHub APK نمی‌سازد مگر خودتان بخواهید.** با هر push فقط `checks.yml`
می‌دود (بررسی‌ها، آزمون‌های پول، کامپایلِ ویندوز). اگر دستی «ساخت APK» را
اجرا کنید، یک APKِ **دیباگ** می‌سازد برای آزمایش و نسخهٔ release را هم
فقط برای اطمینان از کامپایل شدن — آن بی‌امضاست و روی گوشی نصب نمی‌شود.

نسخهٔ نصبی **همیشه** از Android Studio می‌آید. همان مسیری که در گام ۲
نوشته شده.
