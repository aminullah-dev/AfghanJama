# راهنمای AfghanJama

این راهنما کوتاه است چون خودِ اپ باید واضح باشد، نه دفترچه‌اش.

---

## روزِ اول

قبل از ثبتِ اولین سفارش، یک بار این‌ها را وارد کنید. بعد از این دیگر لازم نیست:

۱. **اطلاعات پایه** → نامِ خیاط‌ها، ناظرها، نوعِ پارچه‌ها، رنگ‌ها و سایزها.
۲. **کارکنان** → هر کسی که حقوقِ ماهانه می‌گیرد، با مبلغِ توافقی‌اش.
۳. **انبار مواد** → آنچه همین حالا در انبار دارید، با تعداد و قیمت.

اگر چیزی جا افتاد نگران نباشید؛ هر جا لازم شود همان‌جا می‌شود اضافه کرد.

---

## یک سفارش، از اول تا آخر

```
ثبت سفارش → برش → دوخت → نظارت → آمادهٔ تحویل → تحویل شد
```

**۱. ثبت سفارش** — نامِ مشتری، طرح، تعداد، پارچه، قیمتِ توافقی و مهلتِ تحویل.
اگر مشتری بیعانه داد، همان‌جا ثبتش کنید.

**۲. برش** — مسئولِ برش را بزنید و تعدادِ دستِ برش‌شده را وارد کنید.
اندازه‌های مشتری بالای همین صفحه نوشته شده. **موادِ سفارش دقیقاً همین‌جا از انبار کم می‌شود**، نه موقعِ ثبتِ سفارش — چون مصرفِ واقعی در برش اتفاق می‌افتد.

**۳. دوخت** — کار را بین خیاط‌ها تقسیم کنید، هر کدام با تعداد و کارمزدِ فی‌عدد.
یک سفارش می‌تواند بین چند خیاط تقسیم شود. دکمهٔ «رسید» یک پیام آماده می‌سازد که اندازه‌ها و کارمزد در آن هست — برای خیاط بفرستید.

**۴. نظارت** — ناظر تأیید می‌کند یا برای اصلاح برمی‌گرداند.
اگر برگرداند و سفارش بینِ چند خیاط تقسیم شده باشد، اپ می‌پرسد **کارِ کدام خیاط برگشت خورده**. جوابش را بدهید؛ همین یک کلیک است که کارنامهٔ خیاط‌ها را درست نگه می‌دارد. اگر واقعاً معلوم نیست، «معلوم نیست» را بزنید — به گردنِ کسی انداخته نمی‌شود.

**۵. آمادهٔ تحویل** — کارِ تأییدشده خودکار وارد انبار محصول می‌شود و در صفحهٔ «آمادهٔ تحویل» می‌آید.

**۶. تحویل** — دکمهٔ «تحویل» را بزنید. قیمت را می‌آورد، بیعانه را پیشنهاد می‌دهد، نقدِ دریافتی را می‌گیرد و باقی‌ماندهٔ طلب را نشان می‌دهد. بعدش رسیدِ تحویل برای مشتری قابلِ فرستادن است.

---

## پول

| کار | کجا |
|---|---|
| پرداخت به هر کسی (خیاط، کارمند، تأمین‌کننده) | داشبورد ← **پرداخت** |
| دریافت از هر کسی | داشبورد ← **دریافت** |
| خرید، فروش، برگشت از خرید، برگشت از فروش | داشبورد ← **معاملات روزمره** |
| حقوقِ ماهانه | **حقوق کارکنان** |
| کی به ما بدهکار است، ما به کی | **دفتر کل** |
| سود و زیان، ترازنامه، گزارش‌ها | **گزارش‌ها** |

**بیعانه:** پولی که پیش از تحویل می‌گیرید بدهیِ شماست، نه درآمد. اپ خودش این را می‌داند و موقعِ تحویل پیشنهاد می‌دهد روی همان فروش اعمالش کنید. دو بار حساب نمی‌شود.

**پیش‌پرداختِ کارمند:** اگر کارمندی پیش از موعد پول خواست، از «پرداخت» ثبتش کنید. به‌عنوان طلبِ شما از او می‌ماند و از حقوقش کم می‌شود.

---

## عادت‌های خوب

**هر روز صبح — مرکز هشدار.** سفارشِ معطل، مهلتِ نزدیک، موادِ رو به اتمام، کارمزدِ تسویه‌نشده و کارِ آماده‌ای که مشتری نبرده — همه یک‌جا. اگر روزی فقط یک صفحه را باز می‌کنید، همین باشد.

**هفته‌ای یک بار — پشتیبان.** تنظیمات ← پشتیبان‌گیری. فایل را جایی بیرون از گوشی نگه دارید. گوشی گم می‌شود؛ دفترِ حساب نباید گم شود.

**اندازه‌ها را در صفحهٔ مشتری وارد کنید.** یک بار وارد کنید، در برش و دوخت و رسیدِ خیاط خودش می‌آید.

**قیمتِ توافقی را موقعِ ثبتِ سفارش بنویسید.** اگر ننویسید، موقعِ تحویل باید از حافظه بگویید و همان‌جاست که اختلاف پیش می‌آید.

---

## چیزهایی که ممکن است گیج‌کننده باشد

**چرا سفارشِ تأییدشده وارد «انبار محصول» می‌شود؟**
چون تا وقتی دستِ مشتری نرسیده، مالِ کارگاه است و باید در موجودی دیده شود. اگر مشتری داشته باشد، از «آمادهٔ تحویل» تحویلش بدهید؛ اگر برای فروشِ عمومی دوخته شده، از «انبار محصول» بفروشید.

**چرا اسکنر گاهی کد می‌خواهد؟**
اگر دوربین بالا نیامد، اپ به‌جای اینکه ساکت بماند از شما کدِ سفارش را می‌پرسد. کدِ کوتاه هم قبول است.

**کارنامهٔ خیاط چرا بعضی سفارش‌ها را حساب نکرده؟**
فقط وقتی سفارشی بینِ چند خیاط تقسیم شده و ناظر نگفته کارِ کدام برگشت خورده. تعدادش پایینِ همان صفحه نوشته می‌شود.

---

## نقش‌ها

هر کس فقط چیزی را می‌بیند که به کارش می‌آید. خیاط «کارِ من» را می‌بیند — کارِ زیرِ دستش و کارمزدش. فهرستِ مشتریان و شماره‌هایشان فقط برای مدیر و بخشِ فروش باز است.

روی گوشیِ خیاط، یک بار در تنظیمات مشخص کنید دستِ کدام خیاط است — از روی نامِ ورود حدس زده نمی‌شود، چون دو خیاطِ هم‌نام کارِ همدیگر را می‌دیدند.

---

## دکمهٔ قهوه‌ای «خودآزمایی» (موقتی)

بالای داشبورد یک نوارِ قهوه‌ای هست به نامِ **خودآزمایی**. رنگش عمداً جزوِ
رنگ‌های اپ نیست تا یادتان بماند موقتی است.

بزنیدش، خودش اجرا می‌شود و می‌گوید همه‌چیز درست است یا نه. دو کار می‌کند:
ریاضیِ پول و کارنامه را می‌سنجد، و دفترِ واقعیِ همین گوشی را وارسی می‌کند —
مثلاً اینکه هر سندِ حسابداری خودش تراز باشد و هیچ دارایی ماندهٔ منفی نداشته
باشد.

**چیزی نمی‌نویسد.** نه سفارشِ آزمایشی می‌سازد نه سندی ثبت می‌کند؛ فقط
می‌خواند. هر وقت خواستید بزنید، مخصوصاً بعد از هر به‌روزرسانی.

اگر چیزی قرمز شد، دکمهٔ اشتراک بالای صفحه متنش را می‌فرستد.

هر وقت گفتید، این دکمه و صفحه‌اش با یک کامیت حذف می‌شود.
