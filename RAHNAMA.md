# راهنمای خیاط‌یار

این راهنما کوتاه است چون خودِ اپ باید واضح باشد، نه دفترچه‌اش.

---

## روزِ اول

روی اندروید، کارگاهِ کاملاً خالی اول به صفحهٔ **«شروعِ کارگاه»** می‌رود و
«کارگاهِ نمونه» را می‌سازد: طرح، پارچه، رنگ، سایز، خرجِ کار، خیاط، ناظر،
کارمند، مشتری، و یک ماه کار و حساب. هر کارگاه از همین نقطهٔ استاندارد شروع
می‌کند. (گوشیِ تازه برای کارگاهی که پشتیبان دارد؟ همان صفحه دکمهٔ «بازیابی از
فایل پشتیبان» دارد؛ نمونه نسازید.) بعد:

۱. **از آنِ خودتان کنید** → اطلاعات پایه (نام و قیمت‌ها)، «حقوق کارکنان» ←
   ویرایش (نام، سمت، حقوق، یا حذف)، و پروندهٔ هر مشتری ← آیکنِ مداد (نام،
   تلفن، یا حذف).
۲. **دادهٔ ساختگی را بیرون ببرید** → تنظیمات ← «پاک‌کردن کارها و حساب‌ها».
   سفارش‌ها، پول و انبارِ نمونه می‌روند؛ آنچه شخصی کردید می‌ماند و گزارشِ
   مالی از صفر شروع می‌شود.
۳. **انبار مواد** → آنچه همین حالا دارید، با تعداد و قیمت.

**تغییرِ نام، سابقه را هم می‌برد.** سفارش، دفتر، دریافت، قسط، حضور، حقوق و
کارمزدِ آدم‌ها با نام ثبت است؛ وقتی نامِ مشتری، کارمند، خیاط، ناظر یا
فروشنده را عوض می‌کنید، همهٔ این‌ها با آن می‌روند و حساب دو تکه نمی‌شود. به
نامی که از قبل هست نمی‌رود — دو حساب بی‌صدا یکی نمی‌شوند. **حذف** فقط برای
کسی است که هنوز سابقه ندارد. نامِ فروشنده از دفتر کل عوض می‌شود.

**ساختنِ دوبارهٔ کارگاهِ نمونه قفل است.** از لحظه‌ای که کارگاه چیزی دارد،
تنظیمات ← کارگاهِ نمونه رمزِ ورود می‌خواهد. دوباره ساختن کارگاه را به نمونه
برنمی‌گرداند؛ نسخهٔ دیگری از سفارش‌ها و پولِ ساختگی را روی کارِ واقعی اضافه
می‌کند.

---

## یک سفارش، از اول تا آخر

```
ثبت سفارش → برش → دوخت → نظارت → آمادهٔ تحویل → تحویل شد
```

**۱. ثبت سفارش** — نامِ مشتری، طرح، تعداد، پارچه، قیمتِ توافقی و مهلتِ تحویل.
اگر مشتری بیعانه داد، همان‌جا ثبتش کنید.

**۲. برش** — مسئولِ برش را بزنید و تعدادِ دستِ برش‌شده را وارد کنید.
اندازه‌های مشتری بالای همین صفحه نوشته شده. **موادِ سفارش دقیقاً همین‌جا از انبار کم می‌شود**، نه موقعِ ثبتِ سفارش — چون مصرفِ واقعی در برش اتفاق می‌افتد.

**۳. دوخت** — کار را بین خیاط‌ها تقسیم کنید، هر کدام با تعداد و کارمزدِ فی‌عدد.
یک سفارش می‌تواند بین چند خیاط تقسیم شود. دکمهٔ «رسید» یک پیام آماده می‌سازد که اندازه‌ها و کارمزد در آن هست — برای خیاط بفرستید.

**لازم نیست منتظرِ تمام شدنِ کلِ سفارش بمانید.** دکمهٔ «ارسال به نظارت (هرچه آماده است)» هر تعداد که تا آن لحظه دوخته شده را جلو می‌برد — یک عدد، دو عدد، هر چند تا — و باقی در دوخت می‌ماند. بعداً که چند تای دیگر آماده شد، همان دکمه را دوباره بزنید. مثلِ نانوا که هر نان که پخت تحویل می‌دهد در حالی که در تنور هنوز نانِ نیم‌پخته هست.

**۴. نظارت** — ناظر تأیید می‌کند یا برای اصلاح برمی‌گرداند.
روی کارتِ نظارت نوشته شده **این ارسال چند عدد است** — تأیید فقط همان تعداد را وارد انبار می‌کند، نه کلِ سفارش. اگر باقیِ سفارش هنوز در دوخت باشد، سفارش به مرحلهٔ دوخت برمی‌گردد و منتظرِ دستهٔ بعدی می‌ماند؛ فقط وقتی همهٔ عددهایش از نظارت گذشتند بسته می‌شود.
اگر برگرداند و سفارش بینِ چند خیاط تقسیم شده باشد، اپ می‌پرسد **کارِ کدام خیاط برگشت خورده**. جوابش را بدهید؛ همین یک کلیک است که کارنامهٔ خیاط‌ها را درست نگه می‌دارد. اگر واقعاً معلوم نیست، «معلوم نیست» را بزنید — به گردنِ کسی انداخته نمی‌شود.

**۵. آمادهٔ تحویل** — کارِ تأییدشده خودکار وارد انبار محصول می‌شود و در صفحهٔ «آمادهٔ تحویل» می‌آید.

**۶. تحویل** — دکمهٔ «تحویل» را بزنید. **تعداد و قیمت هر دو دستِ شماست**، پس اگر مشتری امروز فقط بخشی را می‌برد همان تعداد را بزنید؛ سفارش با باقی‌مانده در صف می‌ماند. مبلغ خودش از تعداد × قیمت ساخته می‌شود. بیعانه پیشنهاد می‌شود و باقی‌ماندهٔ طلب زنده نشان داده می‌شود.

**نکتهٔ مهم دربارهٔ موجودی:** انبارِ محصول برای هر **طرح** یک ردیف دارد، نه برای هر سفارش. یعنی دو سفارشِ هم‌طرح از یک موجودی برمی‌دارند و ممکن است اولی سهمِ دومی را خالی کند. برای همین موجودیِ انبار هم روی کارتِ صف و هم داخلِ دیالوگ نوشته می‌شود.

---

## فروش با چند طرح در یک فاکتور

از **معاملات روزمره ← فروش جدید**. یک فاکتور می‌تواند هر تعداد طرحِ متفاوت
داشته باشد — مثلاً دو پیراهن، یک واسکت و سه دستمال، همه در یک فاکتور و با
یک شمارهٔ فاکتور.

هر ردیف را با «انتخاب کالا» بردارید، تعداد و فی را بنویسید، و برای ردیفِ
بعدی «ردیف جدید» را بزنید. پایینِ صفحه جمعِ کل، بیعانه، نقدِ دریافتی و
ماندهٔ طلب زنده حساب می‌شود.

برگشت از فروش هم ردیف‌به‌ردیف است: می‌توانید فقط یکی از سه قلمِ همان
فاکتور را برگردانید.

---

## پول

| کار | کجا |
|---|---|
| پرداخت به هر کسی (خیاط، کارمند، تأمین‌کننده) | داشبورد ← **پرداخت** |
| دریافت از هر کسی | داشبورد ← **دریافت** |
| خرید، فروش، برگشت از خرید، برگشت از فروش | داشبورد ← **معاملات روزمره** |
| حقوقِ ماهانه | **حقوق کارکنان** |
| کی به ما بدهکار است، ما به کی | **دفتر کل** |
| سود و زیان، ترازنامه، گزارش‌ها | **گزارش‌ها** |

**بیعانه:** پولی که پیش از تحویل می‌گیرید بدهیِ شماست، نه درآمد. اپ خودش این را می‌داند و موقعِ تحویل پیشنهاد می‌دهد روی همان فروش اعمالش کنید. دو بار حساب نمی‌شود.

**پیش‌پرداختِ کارمند:** اگر کارمندی پیش از موعد پول خواست، از «پرداخت» ثبتش کنید. به‌عنوان طلبِ شما از او می‌ماند و از حقوقش کم می‌شود.

---

## وضعیت مالی و پیشنهادِ هوشمند

**مالی ← گزارش**، بالای صفحه: **وضعیت مالی**. چهار عدد — نقد، طلب از
دیگران، موجودیِ کالا، بدهی‌ها — و «خالصِ دارایی». زیرش می‌گوید نقدِ
امروز هزینهٔ چند روزِ کارگاه را می‌دهد و فروشِ این ۳۰ روز نسبت به ۳۰
روزِ قبل چقدر بالا یا پایین رفته. برچسبِ «سالم / زیرِ نظر / خطر» و
جمله‌های زیرش صریح می‌گویند کجا را ببینید. دکمهٔ فرستادن همین خلاصه را
برای شریک یا حسابدار می‌فرستد.

«هزینه‌های کارگاه (۳۰ روز)» حالا همان عددِ صورتِ سود و زیان است. پیش‌تر
خریدِ مواد و انتقال از صندوق به بانک را هم هزینه می‌شمرد و بزرگ‌تر از
واقعیت بود.

**انبار محصول**، بالای فهرست: **پیشنهادِ هوشمند**. دو فهرست:
- **دوباره بدوزید** — طرح‌هایی که با سرعتِ فروشِ ۳۰ روزِ اخیر کمتر از دو
  هفته دیگر تمام می‌شوند، با تعدادِ پیشنهادی. فهرست را می‌شود برای
  سرخیاط فرستاد.
- **راکد** — کالایی که ۶۰ روز یک عدد هم نفروخته، و پولی که در آن خوابیده.

همین‌ها در **مرکز هشدار** هم می‌آیند.

---

## پیگیریِ طلب

**خانه ← پیگیری طلب.** جوابِ «امروز به کی زنگ بزنم؟». هر نفر یک سطر
است با مبلغ و **یک دلیل**، و ترتیب از دلیل می‌آید:

1. **قولش گذشت** — گفته بود تا فلان روز می‌دهد و از آن روز چیزی نرسیده.
2. **امروز روزِ قولش است.**
3. **قسطش گذشته.**
4. **بدهیِ مانده** — قدیمی‌ترین بدهیِ پرداخت‌نشده‌اش دو هفته یا بیشتر است.

سنِ بدهی از **قدیمی‌ترین بدهیِ پرداخت‌نشده** حساب می‌شود، نه از آخرین
پرداخت: کسی که ۴۵ روز پیش نسیه برده و هفتهٔ پیش کمی داده، هنوز بدهیِ
۴۵ روزه دارد. سفارشی که هنوز دوخته نشده «طلب» حساب نمی‌شود — به کسی که
لباسش آماده نیست زنگ زده نمی‌شود.

روی هر سطر:
- **تماس** — شماره را می‌گیرد و بعد می‌پرسد «چه شد؟».
- **پیام** — متنِ مؤدبِ آماده با نامِ کارگاه و مبلغ، برای واتس‌اپ یا پیامک.
- **نتیجه** — «قول داد تا امروز / فردا / ۳ روز / یک هفته / دو هفته»،
  «جواب نداد»، یا «صحبت شد، قولی نداد».

کسی که قول داده تا روزِ قولش به بخشِ **بعداً** می‌رود و زنگ نمی‌خورد.
اگر روزش گذشت و چیزی نداد، خودش بالای فهرست برمی‌گردد. اگر بخشی را داد،
سرِ قولش ایستاده و فقط بقیه‌اش مثلِ هر بدهیِ دیگری پیگیری می‌شود.

کسی که امروز به او سر زده‌اید پایینِ فهرست می‌رود، ولی در آن می‌ماند.
**مرکز هشدار** تا وقتی کسی مانده می‌گوید «امروز N نفر را پیگیری کنید» و
وقتی به همه سر زدید خاموش می‌شود.

> این بخش به تیکِ **مشتریان** بسته است (کاربران و دسترسی‌ها).

---

## پیش‌بینیِ تحویل

اپ سرعتِ واقعیِ کارگاه را از دوخت‌های تحویل‌شدهٔ ۳۰ روزِ گذشته می‌سنجد
(«روزی چند دست») و کارهای باز را **به ترتیبِ مهلت** در صف می‌چیند. از
همین حساب سه چیز درمی‌آید:

- **سفارشِ تازه** — زیرِ «مهلت تحویل» می‌نویسد این سفارش با کارهای فعلی
  حدودِ کِی آماده می‌شود. اگر مهلتی که گذاشته‌اید کم است، قرمز می‌گوید چند
  روز؛ اگر این مهلت سفارشِ دیگری را دیر می‌کند، کدش را می‌گوید. دکمهٔ
  **«مهلتِ مطمئن: N روز»** زودترین مهلتی را می‌گذارد که نه این سفارش دیر
  شود نه هیچ سفارشِ دیگری.
- **بارِ کارگاه** — فهرستِ سفارش‌هایی که هنوز مهلت دارند ولی با این سرعت
  دیر آماده می‌شوند، و کارِ زیرِ دستِ هر خیاط با سرعتِ خودش (کم‌کارترین
  بالا). همین در **مرکز هشدار** هم می‌آید — چند روز **پیش از** گذشتنِ
  مهلت، وقتی هنوز می‌شود کاری کرد.
- **دوخت ← تحویل به خیاط** — کنارِ نامِ هر خیاط نوشته می‌شود چند دست
  زیرِ دست دارد و حدودِ چند روز کار؛ کم‌کارترین اول می‌آید.

اگر هنوز تحویلِ دوختی ثبت نشده، هیچ عددی نشان داده نمی‌شود — اپ حدس
نمی‌زند. دستی که دوختش تمام شده و فقط منتظرِ نظارت است، کارِ دوخت حساب
نمی‌شود.

---

## پیشنهادِ قیمت

فیلدِ قیمت خودش پر نمی‌شود — قیمت تصمیمِ شماست. ولی زیرش چند عددِ
**زدنی** می‌آید، هر کدام با منبعش:

- **آخرین فروش** — همین کالا (همین طرح و سایز) آخرین بار چند رفت.
- **معمول** — میانهٔ قیمتِ فروش‌های اخیرِ همین کالا. یک فروشِ حراجی آن را
  نمی‌کشد.
- **سودِ معمول N٪** — بهای تمام‌شدهٔ **امروز** به‌اضافهٔ درصدی که کارگاه
  معمولاً سود می‌برد (از فروش‌های ۶ ماهِ اخیر، پس از تخفیف).

اگر پارچه یا دستمزد گران شده و قیمتِ همیشگی دیگر سود ندارد، قرمز
می‌گوید. هیچ‌کدام بی تاریخچه ساخته نمی‌شود.

**فاکتورِ فروش:** زیرِ «فی» هر ردیف.
**سفارشِ تازه:** زیرِ «قیمت توافقی» — اول بهای برآوردیِ همین سفارش
(مواد و خرج‌کارِ همین فرم، به‌اضافهٔ دستمزدِ دوختِ معمولِ همین طرح)،
بعد قیمت‌های پیشنهادی برای کلِ تعداد. قیمتِ زیرِ بها همان‌جا قرمز
می‌شود، نه بعد از دوخت.

---

## نگهبانِ خطا

پیش از ثبتِ پول، اگر چیزی شبیهِ اشتباه باشد، اپ یک لحظه نگه می‌دارد و
می‌پرسد — **منع نمی‌کند**؛ «درست است، ثبت کن» همیشه هست.

- **تکرار** — همین مبلغ برای همین نفر امروز هم ثبت شده (مثلاً یک بار روی
  گوشی و یک بار روی پی‌سی). فاکتورِ فروش یا خریدی که با همان جمع برای
  همان مشتری/فروشنده امروز ثبت شده هم همین‌طور.
- **یک صفرِ اضافه یا کم** — ۱۵٬۰۰۰ به‌جای ۱٬۵۰۰. دکمهٔ «… را بگذار» مبلغِ
  درست را خودش می‌نویسد.
- **مبلغِ غیرعادی** — چند برابرِ معمولِ همین نفر (یا اگر تازه است، معمولِ
  همین نوع: کارمندان، مشتریان…).

«معمول» میانهٔ ثبت‌های اخیر است و بی نمونهٔ کافی (۳ ثبت برای یک نفر، ۵
برای یک نوع) هیچ هشداری از این جنس داده نمی‌شود.

کجا: **پرداخت / دریافت**، **دفتر کل ← ثبت سند دستی**، **فاکتور فروش**
(قیمتِ هر ردیف با فروش‌های گذشتهٔ همان کالا)، و **خرید مواد** (قیمتِ واحد با
خریدهای گذشتهٔ همان قلم).

---

## عادت‌های خوب

**هر روز صبح — مرکز هشدار.** سفارشِ معطل، مهلتِ نزدیک، موادِ رو به اتمام، کارمزدِ تسویه‌نشده و کارِ آماده‌ای که مشتری نبرده — همه یک‌جا. اگر روزی فقط یک صفحه را باز می‌کنید، همین باشد.

**هفته‌ای یک بار — پشتیبان.** تنظیمات ← «پشتیبان‌گیری کامل». فایل را جایی بیرون از گوشی نگه دارید. گوشی گم می‌شود؛ دفترِ حساب نباید گم شود.

> اپ هر روز خودش هم یک پشتیبان در `Downloads/KhayatYar` می‌گذارد، ولی آن **فقط دیتابیس** است و روی همین گوشی می‌ماند — اگر گوشی گم شود با آن هم گم می‌شود. پشتیبانی که خودتان می‌گیرید یا می‌فرستید **عکس‌ها را هم دارد**. همان است که واقعاً نجاتتان می‌دهد.

> برای بازیابی همان فایل را در «بازیابی از فایل پشتیبان» انتخاب کنید. پشتیبان‌های قدیمی‌تر (قبل از عکس‌ها) هم باز می‌شوند — چیزی بی‌مصرف نشده.

**اندازه‌ها را در صفحهٔ مشتری وارد کنید.** یک بار وارد کنید، در برش و دوخت و رسیدِ خیاط خودش می‌آید.

**قیمتِ توافقی را موقعِ ثبتِ سفارش بنویسید.** اگر ننویسید، موقعِ تحویل باید از حافظه بگویید و همان‌جاست که اختلاف پیش می‌آید.

---

## چیزهایی که ممکن است گیج‌کننده باشد

**چرا سفارشِ تأییدشده وارد «انبار محصول» می‌شود؟**
چون تا وقتی دستِ مشتری نرسیده، مالِ کارگاه است و باید در موجودی دیده شود. اگر مشتری داشته باشد، از «آمادهٔ تحویل» تحویلش بدهید؛ اگر برای فروشِ عمومی دوخته شده، از «انبار محصول» بفروشید.

**چرا اسکنر گاهی کد می‌خواهد؟**
اگر دوربین بالا نیامد، اپ به‌جای اینکه ساکت بماند از شما کدِ سفارش را می‌پرسد. کدِ کوتاه هم قبول است.

**کارنامهٔ خیاط چرا بعضی سفارش‌ها را حساب نکرده؟**
فقط وقتی سفارشی بینِ چند خیاط تقسیم شده و ناظر نگفته کارِ کدام برگشت خورده. تعدادش پایینِ همان صفحه نوشته می‌شود.

---

## کاربران و دسترسی‌ها

مدیر برای **هر نفر** مشخص می‌کند به کدام بخش‌های اپ راه دارد:
تنظیمات ← **«کاربران و دسترسی‌ها»** ← «کاربرِ تازه».

۱. **نام** — بهتر است همان نامِ خیاط یا کارمند باشد (از فهرست پیشنهاد
   می‌شود)، تا رویدادها و «کارِ من» او را بشناسند.
۲. **رمز** — دست‌کم ۴ رقم، مخصوصِ همین نفر. رمزِ مدیر یا رمزِ نفرِ دیگر
   پذیرفته نمی‌شود، چون در صفحهٔ ورود رمز است که می‌گوید کیست.
۳. **تیک‌ها** — برش، دوخت، نظارت، آمادهٔ تحویل، سفارش‌ها، فروش، مشتریان،
   انبار مواد، خرید، مالی، دفتر کل، حقوق، حضور، اطلاعات پایه، و کارهای
   حساس (ویرایش و حذفِ سفارش، برگشتِ فروش، تسویهٔ کارمزد، اصلاحِ موجودی،
   پشتیبان‌گیری). پیش‌تنظیم‌های «برشکار»، «خیاط»، «ناظر»، «فروشنده» و
   «خرید و انبار» با یک لمس تیک‌ها را می‌زنند؛ بعد هر کدام را عوض کنید.

آن نفر در صفحهٔ ورود **فقط رمزِ خودش** را می‌زند. نوارِ پایین، کاشی‌های
خانه و نوارِ کناریِ پی‌سی فقط بخش‌هایی را نشان می‌دهند که تیک دارد؛ اگر از
راهِ دیگری (هشدار، جست‌وجو) به بخشی برسد که تیکش را ندارد، «به این بخش
دسترسی ندارید» می‌بیند. موجودیِ صندوق و بانک روی خانه فقط برای کسی است که
تیکِ مالی دارد.

**مدیر همیشه همه‌چیز دارد.** پاک‌کردنِ کارها و حساب‌ها، کارگاهِ نمونه،
خودآزمایی و همین صفحه فقط مالِ مدیرند و تیکی برایشان نیست.

**روی همین دستگاه.** کاربران و تیک‌ها روی گوشی یا پی‌سی‌ای ذخیره می‌شوند
که مدیر آن‌ها را ساخته — دستگاهی که چند نفر با آن کار می‌کنند. گوشیِ
شخصیِ یک کارگر جداست و این فهرست را ندارد.

دستگاهی که پیش از این نسخه با نقشِ «دوخت»، «نظارت»، «فروش» یا «خرید» راه
افتاده، همان چیزی را می‌بیند که قبلاً می‌دید.

## نقش‌ها

خیاط «کارِ من» را می‌بیند — کارِ زیرِ دستش و کارمزدش.

روی گوشیِ خیاط، یک بار در تنظیمات مشخص کنید دستِ کدام خیاط است — از روی نامِ ورود حدس زده نمی‌شود، چون دو خیاطِ هم‌نام کارِ همدیگر را می‌دیدند.

---

## کارتِ کارمند و اسکنِ حضور

فقط مدیر. صفحهٔ **حضور و غیاب** ← نشانِ کارت بالای صفحه (یا دکمهٔ
«کارت‌ها»). برای هر خیاط، ناظر و کارمندی که در «اطلاعات پایه» هست یک
کارت با QR ساخته می‌شود. «چاپ یا فرستادنِ همهٔ کارت‌ها» یک PDF می‌دهد:
هشت کارت در هر برگهٔ A4، اندازهٔ کارتِ بانکی، لبهٔ هر کارت خطِ برش.

**سرِ در:** کارمند کارت را نشان می‌دهد، مدیر اسکن می‌کند. ورود یا خروج
بودنش خودش تشخیص داده می‌شود — اگر داخل است خروج می‌خورد، اگر بیرون
است ورود. نام و ساعتِ ثبت‌شده همان لحظه روی صفحه می‌آید.

- **گوشی:** «اسکن با دوربین». سرِ صبح «اسکنِ پیاپی» را روشن کنید تا
  بعد از هر ثبت، دوربین برای نفرِ بعدی خودش باز شود. برای تمام کردن،
  در دوربین دکمهٔ برگشت را بزنید.
- **کمپیوتر:** یک اسکنرِ بارکدِ USB (از نوعی که QR می‌خوانَد) وصل کنید.
  فیلدِ «کدِ کارت» خودش آماده است؛ اسکنر کد را تایپ و ثبت می‌کند. اگر
  ویندوز روی زبانِ فارسی است هم کار می‌کند.
- **بی اسکنر:** کدِ زیرِ QR (مثل `ky31235`) را تایپ کنید.

دو اسکنِ یک نفر در کمتر از یک دقیقه یک بار حساب می‌شود — کارتی که دو
بار جلوی دوربین رفت، ورودِ تازه را خروج نمی‌کند.

کارت نام را نمی‌بَرد، شناسه را می‌بَرد؛ پس با عوض شدنِ نام باطل نمی‌شود.
ولی اگر کسی را از «اطلاعات پایه» پاک کنید، کارتش دیگر کار نمی‌کند.

**کارت جعل‌ناپذیر نیست.** کسی که الگو را بداند می‌تواند کدِ همکارش را
بسازد، و کارتِ واقعی را هم می‌شود به دیگری داد. نگهبانِ اصلی مدیری است
که پشتِ اسکنر ایستاده و چهره را می‌بیند.

---

## وقت نان و چای

در صفحهٔ **حضور و غیاب**، پایینِ فهرستِ کارمندان، بخشِ «وقت نان و چای» است.

وقت‌های ثابتِ کارگاه را اینجا بنویسید — چای صبح، نان چاشت، چای عصر یا هر
چیز دیگر. اگر تازه شروع کرده‌اید، یک دکمه هر سه‌تای معمول را یک‌جا اضافه
می‌کند و بعد هر کدام را می‌توانید عوض کنید.

سرِ همان ساعت گوشی یادآوری می‌کند و **می‌گوید چند نفر همین حالا داخلِ
کارگاه‌اند** — تا آشپز بداند برای چند نفر آماده کند. این عدد از خودِ حضور
و غیاب می‌آید: هر کسی که ورود زده و خروج نزده.

هر وقت را می‌شود بدونِ حذف‌کردن خاموش کرد (مثلاً چای عصر در رمضان).

---

## تابلوی دوخت (صفحهٔ دیوارِ کارگاه)

داشبورد ← **تابلوی دوخت**. مثلِ تابلوی پروازِ فرودگاه: بزرگ، تیره، از دور
خوانا، و همیشه روشن.

نشان می‌دهد چه کاری زیرِ دستِ کدام خیاط است، چند عدد، چند روز، و چقدر تا
مهلت مانده. **دیرشده‌ها قرمز و بالای فهرست‌اند** — مثلِ پروازِ تأخیردار.
اگر کارها از یک صفحه بیشتر باشند، خودش هر چند ثانیه ورق می‌زند.

تا وقتی این صفحه باز است **گوشی خاموش نمی‌شود**. برای برگشت، صفحه را لمس
کنید.

**بهترین کار:** یک گوشیِ کهنه یا تبلت را روی دیوارِ کارگاه بگذارید و
همین صفحه را بازش کنید. با «اشتراک کارگاه» آن را به گوشیِ اصلی وصل کنید
تا تابلو زنده باشد در حالی که خودتان با گوشیِ خودتان کار می‌کنید.

روی تابلو **نه پول هست نه نامِ مشتری** — چیزی که روی دیوار می‌ماند نباید
حسابِ کسی را لو بدهد.

---

## اشتراک کارگاه (چند گوشی روی وای‌فای)

اگر می‌خواهید خیاط‌ها گوشیِ خودشان را داشته باشند، از داشبورد ←
**اشتراک کارگاه**.

**قانون ساده: یک گوشی دفتر را دارد.** گوشیِ شما «اصلی» می‌شود و تنها
همان می‌فروشد، می‌خرد و پرداخت می‌کند. گوشیِ کارگر فقط کارش را می‌بیند و
«تمام شد» می‌زند — که یک **درخواست** است، نه ثبت. تا شما روی گوشیِ خودتان
تأیید نکنید، هیچ کارمزدی در حساب نمی‌نشیند.

**راه‌اندازی:**
۱. روی گوشیِ خودتان (یا کمپیوترِ دفتر) «این گوشیِ اصلیِ کارگاه است» را
بزنید. یک **QR**، یک **نشانی** و یک **رمزِ شش‌رقمی** نشان می‌دهد.
۲. روی گوشیِ کارگر «اسکنِ QRِ گوشیِ اصلی» را بزنید و QR را بخوانید —
وصل می‌شود، بی تایپ. اگر دوربین نبود، همان نشانی و رمز را دستی وارد کنید.
۳. در تنظیماتِ گوشیِ کارگر مشخص کنید دستِ کدام خیاط است.

اگر دستگاهِ اصلی چند شبکه دارد (مثلاً وای‌فای و VPN، یا کمپیوتری که
VirtualBox دارد)، زیرِ نشانی «نشانی‌های دیگر» هم می‌آید؛ اگر اولی وصل
نشد، آن‌ها را امتحان کنید.

هر دو گوشی باید روی وای‌فای یا هات‌اسپاتِ کارگاه باشند. **اینترنت لازم
نیست** و هیچ داده‌ای از کارگاه بیرون نمی‌رود.

درخواست‌های رسیده در همان صفحه، روی گوشیِ اصلی، منتظرِ تأیید یا رد
می‌مانند. اگر خیاطی یک کار را دو بار اعلام کند، بارِ دوم موقعِ تأیید رد
می‌شود — کارمزد دو بار حساب نمی‌شود.

هر وقت خواستید با «خاموش‌کردن اشتراک» به حالتِ تک‌گوشی برگردید.

---

## چاپِ فاکتور و رسید

هر سندی که اپ می‌سازد — فاکتور فروش، فاکتور خرید، رسیدِ پول، رسیدِ حقوق —
از صفحهٔ **اسناد** باز می‌شود و سه راه دارد:

- **چاپ** — با هر پرینتری که روی گوشی نصب باشد، از جمله پرینترِ حرارتیِ
  ۸ سانتی. اگر پرینتری نصب نیست، همان‌جا «ذخیره در PDF» را می‌بینید.
- **PDF** — برای بایگانی و ایمیل.
- **تصویر** — برای واتساپ. عکس مطمئن‌تر از PDF می‌رسد و مشتری بی‌دردسر
  بازش می‌کند.

بالای دکمه‌ها اندازهٔ کاغذ را انتخاب می‌کنید: **A4**، **A5**، یا
**۸ سانتی**. انتخابتان یادش می‌ماند و برای فاکتور و رسید جدا نگه داشته
می‌شود — چون معمولاً فاکتور روی A5 می‌رود و رسید روی رول.

فاکتور فروش حالا جدولِ کالاها را دارد (کد، نام، تعداد، فی، جمع) و پایینش
سه عدد که مشتری همان‌ها را می‌خواند: **بدهی قبلی**، **پرداخت**، و **مبلغ
قابل پرداخت**. این سه از دفتر کل می‌آیند، نه از حسابِ جداگانه؛ پس برگه
هرگز با دفتر اختلاف پیدا نمی‌کند.

---

## کسری: وقتی جنس نیست ولی مشتری می‌خواهد

گاهی جنس فروخته می‌شود پیش از آنکه کسی ورودش را ثبت کند. اپ جلویتان را
نمی‌گیرد: فروش یا تحویل انجام می‌شود و کمبود با رنگِ **قرمز** به‌عنوان
«کسری» می‌ماند.

کسری یعنی «به این اندازه جنسی فروخته‌ایم که هنوز نداریم». با ورودِ بعدیِ
همان کالا خودش تسویه می‌شود و اگر بهای واقعی با برآورد فرق داشته باشد،
اپ خودش اصلاحش می‌کند. لازم نیست شما کاری بکنید.

اگر ترجیح می‌دهید اپ سخت‌گیر باشد، از **تنظیمات ← فروشِ بیشتر از موجودی**
خاموشش کنید.

---

## تخفیف و عکسِ کالا

هنگام فروش از انبار، یک کادرِ **تخفیف** هست که روی کلِ آن ردیف اعمال
می‌شود. جمعِ فروش و فاکتور هر دو خودشان تخفیف را کم می‌کنند، و اگر مشتری
جنس را پس بیاورد دقیقاً همان چیزی که داده پس گرفته می‌شود — نه بیشتر.

هر کالای انبار هم می‌تواند یک **عکس** داشته باشد. در فهرستِ انبار کنارِ
نامش دیده می‌شود، و فروشنده زودتر از خواندنِ نام می‌فهمد کدام طرح است.

---

## ریست داده — وقتی تست تمام شد

تنظیمات ← پایینِ صفحه، کارتِ قرمز **«پاک‌کردن کارها و حساب‌ها»**. فقط مدیر
می‌بیندش.

**پاک می‌شود:** سفارش‌ها و همهٔ مراحلشان، فروش‌ها، خریدها، دفتر کل، اسناد،
انبار مواد و محصول، حضور و غیاب، و عکس‌ها. شمارهٔ سفارش هم از ۱ شروع
می‌شود.

**می‌ماند:** خیاط‌ها، ناظرها، کارکنان، پارچه‌ها، رنگ‌ها، سایزها، طرح‌ها،
خرج‌کارها، و **مشتری‌ها با اندازه‌هایشان**. یعنی لازم نیست همه‌چیز را
دوباره وارد کنید.

پیش از پاک‌کردن، اپ خودش یک پشتیبانِ **کامل** در `Downloads/KhayatYar`
می‌گذارد. اگر آن نوشته نشود، هیچ چیزی پاک نمی‌شود. پس اگر پشیمان شدید،
همان فایل را بازیابی کنید و همه‌چیز برمی‌گردد.

برای تأیید باید عبارتِ **پاک کن** را بنویسید. این عمدی است: این کار برگشت
ندارد و نباید با یک ضربهٔ اشتباه انجام شود.

---

## خودآزمایی و سلامتِ داده

**تنظیمات → خودآزمایی و سلامتِ داده**

بزنیدش، خودش اجرا می‌شود و می‌گوید همه‌چیز درست است یا نه. دو کار می‌کند:
ریاضیِ پول و کارنامه را می‌سنجد، و دفترِ واقعیِ همین گوشی را وارسی می‌کند —
مثلاً اینکه هر سندِ حسابداری خودش تراز باشد، هیچ دارایی ماندهٔ منفی نداشته
باشد، و ارزشِ انبار با دفتر بخوانَد.

**چیزی نمی‌نویسد.** نه سفارشِ آزمایشی می‌سازد نه سندی ثبت می‌کند؛ فقط
می‌خواند. هر وقت خواستید بزنید، مخصوصاً بعد از هر به‌روزرسانی.

اگر چیزی قرمز شد، دکمهٔ اشتراک بالای صفحه متنش را می‌فرستد.

---

## پروفایل کارگاه

**تنظیمات → پروفایل کارگاه**

نام، لوگو، تلفن و آدرسِ کارگاه. همین‌ها روی **سرصفحهٔ فاکتور**،
**رسیدهایی که در واتساپ می‌فرستید** و **تابلوی کارگاه** می‌نشینند.

تا وقتی نامی ثبت نکرده‌اید، همه‌جا عنوانِ خنثای «کارگاه خیاطی» نوشته
می‌شود — نه نامِ هیچ کارگاهِ دیگری.

لوگو را با دکمهٔ «لوگو» از گالری انتخاب کنید. از وسط مربع می‌شود، پس عکسِ کشیده یا
خوابیده هم روی کاغذ بدشکل نمی‌شود. اگر لوگو نگذارید، نشانِ سوزن و نخِ
خودِ اپ چاپ می‌شود.

> لوگو با «پاک‌کردن کارها و حساب‌ها» از بین نمی‌رود؛ مثلِ نام و تلفن،
> بخشی از هویتِ کارگاه است نه از کارهایش.

---

## شمارشِ انبار محصول

گاهی عددی در انبار ثبت است که در واقعیت نیست. برای اصلاحش:

**انبار محصول → پوشه را باز کنید → کنارِ کالا دکمهٔ «شمارش»**

تعدادی را که واقعاً شمرده‌اید بنویسید. صفر که بزنید، کالا از فهرست
برداشته می‌شود؛ تاریخچهٔ فروش‌هایش سرِ جایش می‌مانَد.

این کار فقط عدد را عوض نمی‌کند — سندِ حسابداری‌اش را هم می‌زند. کم‌شدن
هزینهٔ کسری است و اضافه‌شدن عکسش. برای همین بعد از شمارش، دفتر و انبار
باز هم با هم می‌خوانند و خودآزمایی سبز می‌مانَد.

هر شمارش در **رویدادها** ثبت می‌شود: از چند به چند، چقدر ؋، و توضیحی که
خودتان نوشته‌اید.

---

## عددِ کسری (دو و نیم متر)

در کادرهایی که **مقدار** می‌گیرند — مصرفِ پارچه، موجودی انبار، حدِ
هشدار، ضایعات، مقدارِ خرید و برگشتی — کیبورد نقطه دارد.

`2.5` بنویسید. اگر با کیبوردِ فارسی می‌نویسید، `۲٫۵` و `۲،۵` هم همان
عدد را می‌دهند.

کادرهای **پول** و **تعدادِ لباس** عمداً نقطه ندارند، چون نیم‌عدد لباس
معنا ندارد.
